// Package event will implement prsm's event engine: diffing successive
// snapshots into typed events and dispatching them to subscribers and shell
// hooks (internal/hook). Planned for v1.1; no code lives here yet.
package event
